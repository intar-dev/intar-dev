# Intar run namespaces route through a host-side veth and require forwarding.
# The runtime only verifies this value and fails closed; it never changes a
# host-wide sysctl in response to an agent request.
net.ipv4.ip_forward = 1
