name: Agent Validate
run-name: "${{ github.event_name == 'pull_request' && format('Agent validate PR #{0}', github.event.pull_request.number) || format('Agent validate {0}', github.ref_name) }}"

on:
  pull_request:
    paths:
      - ".github/workflows/agent-ci.yml"
      - ".github/workflows/agent-release.yml"
      - "projects/agent/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/agent-ci.yml"
      - ".github/workflows/agent-release.yml"
      - "projects/agent/**"

permissions:
  contents: read

jobs:
  verify:
    name: Run agent checks
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        working-directory: projects/agent
        run: |
          rustup toolchain install --profile minimal
          rustup component add rustfmt clippy
          rustup show active-toolchain

      - name: Set up Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: projects/agent

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest

      - name: Run checks
        working-directory: projects/agent
        run: just verify
