# Hint 4: Full pre-flight sequence

```bash
cd /opt/platform-engineering-workshop
./scripts/install.sh --check
cd lab/00-setup
./verify.sh
```

Both commands are checks. Do not run the install, initialization, or pull paths. If a
check stays red, use **Need help** and let the facilitator restore checkpoint 00 or
reprovision the workspace.
