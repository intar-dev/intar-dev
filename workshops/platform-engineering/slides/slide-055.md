# GO — Module 07

**Outcome:** an image built in-cluster, in your registry, running.

```bash
# enable zot.yaml + argo-workflows.yaml; submit workflow-run.yaml
cd lab/07-ci && ./verify.sh
```

demo first · then self-paced, ~25 min
