# GO — Module 08

**Outcome:** a database created from a form — provably real.

```bash
# enable portal.yaml → open http://localhost:30600
cd lab/08-portal && ./verify.sh
```

~20 min · then read the source: `apps/portal/`
