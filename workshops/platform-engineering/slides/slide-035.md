# GO — Module 02

**Outcome:** push a commit, watch it materialize. No `kubectl apply`.

```bash
./scripts/bootstrap-gitops.sh && ./scripts/seed-gitea.sh
cd lab/02-gitops && ./verify.sh
```

35 min · behind? `./scripts/catch-up.sh 2`
