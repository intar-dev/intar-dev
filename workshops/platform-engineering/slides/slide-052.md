# GO — Module 06

**Outcome:** watch pods go 0 → 1 → 0 around a `200`.

```bash
# enable knative-serving.yaml; deploy hello-ksvc.yaml via GitOps
cd lab/06-serverless && ./verify.sh
```

~20 min · time the cold start!
