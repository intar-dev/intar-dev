# The lab contract

  Outcome, not stepsthe README says *reach state X*
  Layered hintsfree, collapsible — last one is the answer
  `./verify.sh`checks your live cluster → green = done
  `catch-up.sh &lt;n&gt;`jump to any module's end-state
