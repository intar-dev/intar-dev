# GO — Module 05

**Outcome:** faults 1 + 4 from symptom → verified cause → fix.

```bash
cd lab/05-debug-with-ai
./inject.sh 1        # then 4; ./verify.sh when done
```

25 min · write the diagnosis **before** the fix
