# GO — Module 03

**Outcome:** `psql` into your own DBaaS; a presigned download verified in the learner terminal.

```bash
# enable cnpg-operator.yaml + rustfs.yaml from the catalog
cd lab/03-data && ./verify.sh
```

35 min · behind? `./scripts/catch-up.sh 3`
