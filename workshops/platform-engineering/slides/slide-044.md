# GO — Module 04

**Outcome:** a 10-line YAML → database + bucket appear.

```bash
# enable crossplane.yaml; ship platform/xrd.yaml + composition.yaml
cd lab/04-self-service && ./verify.sh
```

35 min · behind? `./scripts/catch-up.sh 4`
