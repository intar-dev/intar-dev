# GO — Module 09

**Outcome:** upload a photo → a service that wasn't running resizes it.

```bash
# enable knative-eventing.yaml + picture-pipeline.yaml
cd lab/09-capstone && ./verify.sh
```

~25 min · trophy: the trace in Grafana
